---
title: "Regla 0x3001h: Siempre verificá la asignación exitosa de memoria dinámica"
short_title: "0x3001h"
subtitle: "Punteros y Gestión de Memoria (0x30XX)"
---

(0x3001h)=
# 0x3001h: Siempre verificá la asignación exitosa de memoria dinámica

:::{admonition} Categoría: Punteros y Gestión de Memoria (0x30XX)
:class: note
Código de regla: `0x3001h` | Soporta corrección automática: **No**
:::

:::{seealso} Antipatrones didácticos relacionados
* **[AP-0x3001h: Uso de memoria dinámica sin validar retorno a NULL](ap_0x3001h.md)** — desreferenciar el resultado sin comprobarlo.
* **[AP-0x301Bh: Desreferencia inmediata tras realloc](ap_0x301Bh.md)** — perder el puntero original sin validar.
* **[AP-0x301Fh: Asignación de retorno de malloc() a variable no puntero](ap_0x301Fh.md)** — truncar la dirección en un entero.
* **[AP-0x3023h: Comprobación de puntero nulo posterior a su desreferencia](ap_0x3023h.md)** — chequear cuando ya es tarde.
* **[AP-0x3024h: Asignación múltiple a malloc en bucle sin liberación ante fallos parciales](ap_0x3024h.md)** — dejar memoria colgada al fallar una iteración.
* **[AP-0x3026h: Desreferencia condicional de puntero local sin inicializar](ap_0x3026h.md)** — chequear basura en lugar del retorno.
* **[AP-0x3029h: Desreferencia directa tras retorno de realloc sin asignación temporal](ap_0x3029h.md)** — perder el bloque original ante fallo.
:::

## Enunciado normativo

> Toda llamada a `malloc`, `calloc` o `realloc` **DEBE** ser seguida
> inmediatamente por una comprobación del resultado contra `NULL`, **antes** de
> desreferenciar el puntero o de usarlo en cualquier cálculo.

## ¿Por qué existe esta regla?

### El problema

El asignador puede fallar: el *heap* se agota, el sistema niega una página o el
tamaño es irrazonable. Entonces `malloc`, `calloc` y `realloc` devuelven `NULL`,
una dirección que no apunta a ningún objeto válido. Desreferenciarla es
comportamiento indefinido y suele terminar en `SIGSEGV`; la verificación
convierte ese fallo del entorno en un error controlado.

### Consecuencias de violarla

| Tipo de consecuencia | Efecto concreto |
| :--- | :--- |
| Comportamiento indefinido | Desreferenciar `NULL` es UB (C11 §6.5.3.2); suele terminar en SIGSEGV. |
| Bug silencioso | Una cuenta que asume "siempre hay memoria" produce datos corruptos meses después. |
| Robustez | El programa no distingue entre "sin resultados" y "sin memoria". |

### Fundamento en el estándar y en la cátedra

C11 §7.22.3 especifica que las funciones de asignación devuelven un puntero nulo
si no pueden satisfacer la solicitud. La cátedra trata esa condición como parte
del contrato: quien falla debe poder propagarlo, y quien llama, decidir. La regla
no exige abortar, exige **decidir explícitamente**.

## Alcance y excepciones

Aplica a toda asignación dinámica, incluidas las de un constructor de TAD o una
reasignación intermedia. **No** aplica a memoria estática ni a variables
automáticas. El caso `malloc(0)` se discute en casos límite.

## Ejemplos exhaustivos

### ❌ Contraejemplo 1 — Desreferencia inmediata sin comprobar

```c
nodo_t *nuevo = malloc(sizeof(*nuevo));
nuevo->dato = valor;
nuevo->sig = NULL;
```

Por qué falla: si el *heap* no tiene espacio, `nuevo` es `NULL` y la asignación a
`nuevo->dato` escribe en la dirección cero. El chequeo debe ir inmediatamente
después de `malloc`, no al final de la función.

### ❌ Contraejemplo 2 — Comprobación después de usar el recurso

```c
arreglo = malloc(cantidad * sizeof(*arreglo));
inicializar(arreglo, cantidad);
if (arreglo == NULL)
{
    return ERROR_MEMORIA;
}
```

Por qué falla: la verificación llega tarde. `inicializar` recibe un puntero nulo
y lo desreferencia antes de que el `if` se evalúe. La comprobación debe ser
**inmediata**.

### ✅ Ejemplo conforme 1 — Verificación inmediata y propagación

```c
nodo_t *nodo_crear(int valor)
{
    nodo_t *nuevo = malloc(sizeof(*nuevo));
    if (nuevo == NULL)
    {
        return NULL;
    }
    nuevo->dato = valor;
    nuevo->sig = NULL;
    return nuevo;
}
```

La función documenta su contrato: "devuelve `NULL` si no hay memoria". El
llamador decide si aborta, reintenta o degrada la funcionalidad, conforme a
{ref}`0x3009h`.

### ✅ Ejemplo conforme 2 — Envoltorio centralizado de asignación

```c
static void *reservar(size_t cantidad)
{
    void *bloque = malloc(cantidad);
    if (bloque == NULL)
    {
        fprintf(stderr, "Sin memoria para %zu bytes\n", cantidad);
        exit(EXIT_FAILURE);
    }
    return bloque;
}
```

Cuando todo el módulo tiene una política única de fallo (abortar), conviene
centralizarla. Así el chequeo aparece una sola vez y ningún camino lo olvida.
La verificación sigue existiendo aunque el llamador ya no la repita.

### ⚠️ Casos límite

- **`malloc(0)`:** el estándar permite devolver `NULL` o un puntero no nulo
  único. No lo trates como "sin memoria" sin comprobar antes `cantidad == 0`.
- **`calloc(n, m)`:** además de `NULL`, verificá el desbordamiento de `n * m`;
  `calloc` lo detecta y devuelve `NULL`, pero tu lógica debe distinguirlo.
- **`realloc`:** si falla devuelve `NULL` y **conserva** el bloque original; no
  reemplaces el puntero hasta validar (ver {ref}`0x3015h`).

## Cómo detectarla

| Herramienta | Comando | Señal |
| :--- | :--- | :--- |
| `gaff` | `gaff check archivo.c` | `malloc`/`calloc`/`realloc` sin `if (... == NULL)` asociado. |
| `gcc` / `clang` | `gcc -Wall -Wextra -std=c11 -fanalyzer ...` | `dereference of possibly-NULL` en el flujo de asignación. |
| `valgrind` | `valgrind ./programa` | `Invalid write` en dirección `0x0` cuando el fallo se fuerza. |

## Checklist de autocontrol

- [ ] ¿Cada `malloc`, `calloc` y `realloc` tiene su `if (... == NULL)`?
- [ ] ¿La comprobación está inmediatamente después de la llamada?
- [ ] ¿El fallo se propaga como valor de retorno o se maneja explícitamente?
- [ ] ¿Evité desreferenciar antes de comprobar?

## Reglas relacionadas

* {ref}`0x300Bh` — el tamaño se calcula con `sizeof`, no con constantes mágicas.
* {ref}`0x3009h` — la posibilidad de retornar `NULL` debe estar documentada.
* {ref}`0x3015h` — la reasignación segura es un caso particular de esta regla.
