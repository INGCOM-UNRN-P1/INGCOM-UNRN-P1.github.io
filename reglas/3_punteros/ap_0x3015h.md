---
title: "Antipatrón AP-0x3015h: Sobreescritura directa de puntero en realloc"
short_title: "AP-0x3015h"
subtitle: "Punteros y Gestión de Memoria (0x30XX)"
---

(ap_0x3015h)=
# Antipatrón AP-0x3015h: Sobreescritura directa de puntero en realloc

:::{admonition} Regla de Estilo Asociada: 0x3015h
:class: tip
La transgresión descripta en este antipatrón se encuentra normada por la
**[0x3015h: Reallocación segura: no sobreescribir el puntero original directamente](0x3015h.md)**.
:::

:::{admonition} Diagnóstico de Antipatrón
:class: warning
Código de auditoría: `AP-0x3015h` | Categoría: Punteros y Gestión de Memoria (0x30XX)
:::

## Síntoma en el código del estudiante

El retorno de `realloc` se escribe sobre la misma variable que se le pasa:

```c
ptr = realloc(ptr, nuevo_tam);
```

El identificador aparece a ambos lados del `=`, y no existe ninguna variable
temporal.

## Diagnóstico

### Mecanismo del defecto

`realloc` intenta agrandar (o achicar) el bloque. Si lo logra **en el mismo
lugar**, devuelve la misma dirección. Si no puede y consigue otro bloque,
**copia** el contenido y libera el viejo, devolviendo la dirección nueva.

Cuando la reasignación falla, `realloc` devuelve `NULL` **y deja el bloque
original intacto**. Al escribir `ptr = realloc(ptr, n)`, el `NULL` pisa la única
referencia que teníamos al bloque viejo: ni se libera ni se puede volver a
encontrar. Es una **fuga de memoria** irrecuperable, además del `NULL` listo
para ser desreferenciado (ver `AP-0x301Bh`).

### Consecuencia observable

- Si falla: se pierde el puntero original (memory leak) y `ptr` queda en
  `NULL`, con riesgo inmediato de `SIGSEGV` si se usa.
- Si tiene éxito: funciona, por lo que el bug no se manifiesta en el caso feliz
  y solo aparece bajo presión de memoria.
- Detectar la fuga requiere un analizador (Valgrind, LeakSanitizer); el
  compilador no advierte.

## Fundamento en el estándar C11

- C11 §7.22.3.5p3: si `realloc` no puede asignar, devuelve un puntero nulo y el
  bloque original permanece sin cambios (no se libera).
- C11 §7.22.3.5p2: si la reasignación ocurre en un bloque nuevo, el anterior se
  libera y los bytes hasta el mínimo de ambos tamaños se copian.
- La regla 0x3015h cristaliza la única secuencia segura: guardar en temporal,
  verificar y recién entonces reasignar.

## Corrección idiomática

### ❌ Código con el antipatrón

```c
int *crecer(int *ptr, size_t n)
{
    ptr = realloc(ptr, n * sizeof(*ptr));
    return ptr;
}
```

Si `realloc` devuelve `NULL`, el bloque viejo quedó inaccesible.

### ✅ Código refactorizado

```c
int *crecer(int *ptr, size_t n)
{
    int *tmp = realloc(ptr, n * sizeof(*tmp));
    if (tmp == NULL) {
        return ptr;   /* el bloque original sigue siendo válido */
    }
    return tmp;
}
```

### ✅ Variante in situ con liberación del viejo

```c
int *tmp = realloc(ptr, n * sizeof(*tmp));
if (tmp == NULL) {
    free(ptr);
    ptr = NULL;
    return NULL;
}
ptr = tmp;
```

Nótese que primero se comprueba `tmp` y recién después se toca `ptr`.

### ⚠️ Casos límite

- `realloc(ptr, 0)` es equivalente a `free(ptr)`; el retorno puede ser `NULL` y
  no debe tratarse como error de asignación.
- El tipo del temporal debe ser el mismo que el de `ptr` para que
  `sizeof(*tmp)` mida lo correcto.
- Si `ptr` es `NULL`, `realloc(NULL, n)` equivale a `malloc(n)`; sigue valiendo
  la verificación del retorno contra `NULL` (regla 0x3001h).

## Errores típicos al compilar o ejecutar

```text
$ valgrind --leak-check=full ./programa
==1234== LEAK SUMMARY:
==1234==    definitely lost: 40 bytes in 1 blocks
==1234==      at 0x...: malloc (vg_replace_malloc.c)
==1234==      by 0x...: crecer (programa.c:3)
```

## Checklist de verificación

- [ ] ¿`realloc` siempre asigna a una variable temporal?
- [ ] ¿Verifiqué `tmp == NULL` antes de sobrescribir `ptr`?
- [ ] ¿En caso de fallo el bloque original sigue accesible o se liberó?
- [ ] ¿El temporal tiene el mismo tipo que el puntero original?

## Reglas relacionadas

* {ref}`0x3015h` — reallocación segura con temporal.
* {ref}`0x3001h` — verificar el retorno de la reasignación.
* {ref}`0x3002h` — liberar y anular punteros.
* [AP-0x301Bh: Desreferencia inmediata tras realloc](ap_0x301Bh.md) — el `NULL` de `realloc` sin chequear.
