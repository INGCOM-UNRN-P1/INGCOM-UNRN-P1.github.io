---
title: "Antipatrón AP-0x4002h: Control de lectura con while(!feof())"
short_title: "AP-0x4002h"
subtitle: "Gestión de Archivos y Errores (0x40XX)"
---

(ap_0x4002h)=
(0x4002h_ap)=
# Antipatrón AP-0x4002h: Control de lectura con while(!feof())

:::{admonition} Regla de Estilo Asociada: 0x4002h
:class: tip
La transgresión descripta en este antipatrón se encuentra normada por la **[0x4002h: Validá los retornos de las operaciones de lectura y escritura de archivos](0x4002h.md)**.
:::

```{admonition} Diagnóstico de Antipatrón
:class: warning
Código de auditoría: `AP-0x4002h` | Categoría: Gestión de Archivos y Errores (0x40XX)
```


## Diagnóstico
'feof()' solo devuelve verdadero DESPUÉS de que una lectura previa intentó leer más allá del fin de archivo y falló.

## Justificación Pedagógica y de Estándar C
El código de diagnóstico `0x4002h` identifica una práctica desaconsejada en la arquitectura pedagógica de la materia.
Esta transgresión incrementa el acoplamiento, produce comportamiento indefinido o vulnera los invariantes de memoria.

## Sugerencia de Corrección
Controlá el bucle con el valor de retorno de la función de lectura: 'while (fread(...) == 1)' o 'while (fgets(...) != NULL)'.

## Ejemplos

### Código con Antipatrón (Incorrecto)
```c
while (!feof(f)) {
    fread(&elem, sizeof(elem), 1, f);
    procesar(elem);
}
```

### Código Idiomático Refactorizado (Correcto)
```c
while (fread(&elem, sizeof(elem), 1, f) == 1) {
    procesar(elem);
}
```
