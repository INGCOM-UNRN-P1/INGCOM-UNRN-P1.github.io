---
title: "Antipatrón AP-0x1017h: Ramas idénticas duplicadas en bifurcación if-else"
short_title: "AP-0x1017h"
subtitle: "Estructuras de Control y Lazos (0x10XX)"
---

(ap_0x1017h)=
# Antipatrón AP-0x1017h: Ramas idénticas duplicadas en bifurcación if-else

:::{admonition} Regla de Estilo Asociada: 0x1017h
:class: tip
La transgresión descripta en este antipatrón se encuentra normada por la
**[0x1017h: Detector de operadores de incremento o decremento múltiples en una misma expresión](0x1017h.md)**.
:::

:::{admonition} Diagnóstico de Antipatrón
:class: warning
Código de auditoría: `AP-0x1017h` | Categoría: Estructuras de Control y Lazos (0x10XX)
:::

## Síntoma en el código del estudiante

Ambas ramas de un `if` / `else` contienen exactamente la misma sentencia:

```c
if (x > 0)
{
    total += x;
}
else
{
    total += x;
}
```

## Diagnóstico

### Mecanismo del defecto

Si las dos ramas tienen el mismo cuerpo, la condición no discrimina: el
resultado es idéntico sin importar su valor. La estructura de control queda
redundante. El caso típico es un error de copiado al preparar la rama alterna,
o el residuo de una bifurcación que perdió sentido tras un refactor.

### Consecuencia observable

El programa funciona, pero la intención no: el lector busca en vano la
diferencia entre las ramas y sospecha de un bug. Si una de las ramas debía hacer
otra cosa (por ejemplo, restar en lugar de sumar), el defecto es un bug lógico
silencioso que ninguna herramienta señala por defecto.

## Fundamento en el estándar C11

ISO/IEC 9899:2011 §6.8.4.1 permite cualquier par de sentencias en las ramas del
`if`; no es un error sintáctico. La cátedra lo trata como code smell y pide
eliminar la condición o corregir la rama divergente. Se relaciona con
{ref}`0x2005h` (responsabilidad única) y con {ref}`0x1004h` (condiciones con
intención).

## Corrección idiomática

### ❌ Código con el antipatrón

```c
if (x > 0)
{
    total += x;
}
else
{
    total += x;
}
```

### ✅ Código refactorizado

```c
total += x;
```

Si la rama alterna debía hacer otra cosa, se corrige:

```c
if (x > 0)
{
    total += x;
}
else
{
    total -= x;
}
```

La condición recupera su propósito: cada rama hace algo distinto y el `if` deja
de ser decorativo.

## Errores típicos al compilar o ejecutar

```text
No hay advertencia del compilador por defecto. Con -Wall y optimización,
el compilador puede detectar ramas idénticas y colapsarlas:
warning: this 'if' clause does not guard... [-Wmisleading-indentation]
solo en variantes específicas; no es una detección garantizada.

El bug es lógico: si una rama debía restar, el total nunca decrece.
```

## Checklist de verificación

- [ ] ¿Las dos ramas del `if` hacen exactamente lo mismo?
- [ ] ¿La condición aporta alguna diferencia de comportamiento?
- [ ] ¿Una de las ramas tenía un error de copiado?
- [ ] ¿Puedo eliminar el `if` sin cambiar el resultado?

## Reglas relacionadas

* {ref}`0x2005h` — responsabilidad única; ramas idénticas suelen sobrar.
* {ref}`0x1004h` — condiciones que expresan una intención clara.
* {ref}`0x1017h` — regla asociada a este antipatrón.
