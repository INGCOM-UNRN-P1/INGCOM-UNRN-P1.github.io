---
title: "AP032: Retorno prematuro con fuga de recursos de archivo"
short_title: "AP032"
subtitle: "Antipatrones de E/S y Gestión de Archivos"
---

(ap032_4009)=
# AP032 (0x4009h / SP0x4009h): Retorno prematuro con fuga de recursos de archivo

## Diagnóstico
Abandonar la función sin invocar fclose() deja el descriptor de archivo abierto consumiendo recursos del sistema operativo.

## Justificación Pedagógica y de Estándar C
El código de diagnóstico `0x4009h` identifica una práctica desaconsejada en la arquitectura pedagógica de la materia.
Esta transgresión incrementa el acoplamiento, produce comportamiento indefinido o vulnera los invariantes de memoria.

## Sugerencia de Corrección
verificá llamar a 'fclose(f);' antes de cada rama de salida o return.

## Ejemplos

### Código con Antipatrón (Incorrecto)
```c
FILE *f = fopen("data.txt", "r");
if (!f) return -1;
if (error) return -2; // Fuga: f no se cierra
fclose(f);
```

### Código Idiomático Refactorizado (Correcto)
```c
if (error) { fclose(f); return -2; }
```
